# misc

- merge the following todo list into `news.md` when they are done

# infrastructure

- group `.Rmd`s under `inst/` into multiple semantic folders, rename files, update their locations in the R code.
- Add BitBucket package support in metadata
- output image name and container name into a `.yml` file with the same name as the RMD for `drender()` runs.
- make purge functions clean things up based on this `yml` file.
- add a metadata field for including extra custom Dockerfile snippet(s) into the Dockerfile, and support this in `lift()`, add this into vignette.
- convert the metadata field list into a table in vignette.

# ui

- add RStudio addins to create, dockerize, and render RMD files.

# web

- move video to vimeo
- embed video into README
- generate website with pkgdown
